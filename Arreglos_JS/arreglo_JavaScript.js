// Declarar y asignar los valores del arreglo
let numeros = [10, 20, 30, 40];

// Leer el arreglo
console.log(numeros[0]); // 10

// recorrer el arreglo con for
for (let i = 0; i < numeros.length; i++) 
    {
    console.log(numeros[i]);
    }  